# Runtime secrets (chmod 600). NEXT_PUBLIC_* are baked into the web image at build time.
PORT=3000
CLERK_SECRET_KEY=

# Build-time only (set in GitHub Actions secrets for Release workflow):
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# NEXT_PUBLIC_API_GRAPHQL_URL=http://161.97.74.213/mts-api/graphql
# NEXT_PUBLIC_BASE_PATH=/mts
